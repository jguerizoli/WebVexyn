#!/usr/bin/env bash
# DEPRECADO — a verificação/rebuild automático foi substituída pelo Jenkins.
# Mantido apenas como referência histórica.
echo "Script deprecado. O rebuild é feito pelos pipelines do Jenkins." >&2
exit 1
